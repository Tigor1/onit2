spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ONIT
    username: igor
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true